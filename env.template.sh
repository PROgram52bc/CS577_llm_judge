#!/usr/bin/env bash
# Copy this file to env.local.sh (or another untracked file) and update the values.
# Then run `source env.local.sh` before executing experiments to populate secrets.

export OPENAI_API_KEY="replace-with-openai-api-key"
export RCAC_GENAI_API_KEY="replace-with-rcac-api-key"
